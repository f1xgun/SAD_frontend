services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: local
      PORT: 3000
      HOSTNAME: "0.0.0.0"
    restart: always
